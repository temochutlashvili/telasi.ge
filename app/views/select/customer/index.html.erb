<%= table_for @customers, title: 'აბონენტები' do |t|
  t.item_action '#select', select: true
  t.complex_field label: 'აბონენტი' do |c|
    c.text_field :accnumb, tag: 'code'
    c.text_field :custname
  end
  t.paginate records: 'ჩანაწერი'
end %>