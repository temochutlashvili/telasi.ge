<div class="jumbotron">
  <h1>welcome</h1>
</div>
<div class="row">
  <div class="col-sm-6">
    <h3><%= t('dashboard.your_debt') %></h3>
    <p><%= t('dashboard.your_debt_descr') %></p>
    <p class="link"><%= link_to t('dashboard.details').html_safe, customers_url %></p>
  </div>
  <div class="col-sm-6">
    <h3><%= t('dashboard.new_customer') %></h3>
    <p><%= t('dashboard.new_customer_description') %></p>
    <p><%= link_to t('dashboard.details').html_safe, new_customer_url %></p>
  </div>
</div>
<!--div class="homepage">
  <% if current_user.blank? %>
  <div class="register-login">
      <div>
        <div class="row">
          <div class="span6">
            <p><%= t('register.invite.title') %></p>
            <p>
              <%= link_to t('register.invite.action').html_safe, register_url, class: ['btn', 'btn-primary', 'btn-large'] %>
            </p>
            <p style="font-size: 12px; color: #777; padding-right: 50px;">
              <%= t('register.invite.description') %>
            </p>
          </div>
          <div class="span6">
            <p><%= t('login.invite.title') %></p>
            <p>
              <%= link_to t('login.invite.action').html_safe, login_url, class: ['btn', 'btn-success', 'btn-large'] %>
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
  <% end %>
</div -->