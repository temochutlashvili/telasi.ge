<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_url %>"><i class="fa fa-bolt"></i> <%= t('info.telasi') %></a>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="<%= active?('dashboard#index') ? 'active' : 'common' %>"><%= link_to t('menu.home'), root_url %></li>
        <li class="<%= active?('customers#*') ? 'active' : 'common' %>"><%= link_to t('menu.customers'), customers_url %></li>
        <li class="<%= active?('calculator#*') ? 'active' : 'common' %>" data-no-turbolink>
          <%= link_to t('menu.calculator'), calculator_url %>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if current_user.blank? %>
          <li class="<%= active?('dashboard#login,register,restore') ? 'active' : 'common' %>">
            <a href="<%= login_url %>"><i class="fa fa-user"></i> <%= t('menu.login') %></a>
          </li>
        <% else %>
          <li class="dropdown <%= active?('profile#*') ? 'active' : 'common' %>">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <i class="fa fa-user"></i>
              <%= current_user.full_name%>
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to t('menu.profile'), profile_url %></li>
              <li><%= link_to t('models.sys_user.actions.change_password'), change_password_url %></li>
              <li class="divider"></li>
              <li><%= link_to t('menu.logout'), logout_url, data: { confirm: t('menu.logout_confirm') } %></li>
            </ul>
          </li>
        <% end %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-globe"></i></a>
          <ul class="dropdown-menu">
            <li data-no-turbolink><%= link_to 'ქართული', locale_url('ka') %></li>
            <li data-no-turbolink><%= link_to 'Русский', locale_url('ru') %></li>
            <li data-no-turbolink><%= link_to 'English', locale_url('en') %></li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>